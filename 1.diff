diff --git a/app/src/main/java/cc/bbq/xq/ui/plaza/ResourcePlazaScreen.kt b/app/src/main/java/cc/bbq/xq/ui/plaza/ResourcePlazaScreen.kt
--- a/app/src/main/java/cc/bbq/xq/ui/plaza/ResourcePlazaScreen.kt
+++ b/app/src/main/java/cc/bbq/xq/ui/plaza/ResourcePlazaScreen.kt
@@ -243,14 +243,14 @@
 }
 
 @Composable
-private fun CategoryTabs(
+private fun CategoryTabs( 
     categories: List<UnifiedCategory>,
     onCategorySelected: (String?) -> Unit,
-    enabled: Boolean
+    enabled: Boolean,
+    // 可选：添加一个重置触发器，如果未来需要从外部强制重置选择
+    // resetTrigger: Any? = null
 ) {
-    var selectedTabIndex by remember(categories) { mutableIntStateOf(0) }
+    var selectedTabIndex by rememberSaveable(categories) { mutableIntStateOf(0) }
 
     if (categories.isEmpty()) {
         Box(modifier = Modifier.fillMaxWidth().height(48.dp), contentAlignment = Alignment.Center) {
@@ -258,12 +258,6 @@
         return
     }
 
-    // 移除：不应在 categories 变化时重置 selectedTabIndex
-    // LaunchedEffect(categories) {
-    // if(categories.isNotEmpty()) {
-    // selectedTabIndex = 0
-    // }
-    // }
 
     PrimaryScrollableTabRow(
         selectedTabIndex = selectedTabIndex,